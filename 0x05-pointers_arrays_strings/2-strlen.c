#include "main.h"
#include <stdio.h>
/**
 *  _strlen - function gets the string len
 *  @*s - pointer v to the string
 *  Return: return size of the string
 */

int _strlen(char *s)
{
    return ("%ld", strlen(s));
}
