#include "main.h"
#include <stdio.h>

/**
 * mul - takes two numbers and multiplies them
 *
 * &a - first number
 * &b -second number
 * Return: return the product
 */

int mul(int a, int b)
{
	printf("%d", (a * b));
	return (0);
}
