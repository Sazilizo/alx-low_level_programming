#include "main.h"
#include <stdio.h>

/**
 * print_square - function prints squares
 * @c - argument to num of squares
 *
 */

void print_square(int c)
{
	int b , a;

	for (a = 0; a <= c; a++)
	{
  
	    for (b = 0; b <= c ; b++)
        {
		printf("# ");
	}
		printf("\n");
	}
}
